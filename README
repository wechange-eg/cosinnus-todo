============
Installation
============

* Add ``'django.contrib.humanize', 'cosinnus_todo'`` to ``INSTALLED_APPS``
* Optionally set a format for the date picker (Javascript-compatible!) by
setting ``COSINNUS_DATETIME_PICK_FORMAT`` globally or
``COSINNUS_TODO_DATETIME_PICK_FORMAT`` just for this app.
